/**
 * A full-bleed rule between page sections (spec 010 FR-011).
 *
 * Spelled `separator`; the reference file is `PageSeperator` (FR-013).
 */
export default function PageSeparator() {
  return <div className="-mx-8 my-8 h-px bg-border md:-mx-16" />;
}
