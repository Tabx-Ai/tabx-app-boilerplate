{
  "$comment": "The platform contract (constitution Article VIII). This blank template is INJECTED by Architect when the app is generated — read it, never hand-invent it. A capability not listed here is a capability this app does not have. Item shapes inside tools/connections/playbooks are strings for now; the schema tolerates unknown keys so this app stays valid as the platform adds fields. `env` is pre-seeded with TABX_URL because the platform sets it on EVERY app (constitution Article XIV); Architect appends whatever else an app was granted.",
  "name": "",
  "description": "",
  "icon": "",
  "tools": [],
  "connections": [],
  "playbooks": [],
  "sdk": {
    "capabilities": []
  },
  "env": [
    "TABX_URL"
  ]
}
