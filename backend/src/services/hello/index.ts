/**
 * The service's own surface, re-exported (constitution Article IX §8).
 *
 * Re-export only. A fourth file in a service folder is a sign the service is two.
 */
export { helloController } from './controller.js';
